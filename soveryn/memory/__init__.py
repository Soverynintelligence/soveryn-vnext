"""SOVERYN vNext — memory package.

Lattice (semantic graph) and conversation/session persistence. Lattice is the
only semantic memory backend — no ChromaDB, no second vector store (spec §14).
Embedding access is HTTP-only via nomic-embed on :8086.
"""
