"""SOVERYN vNext — tools package.

Declarative tool registry (registry.py) and concrete tool implementations
(implementations/). Tool registration is per-agent and listed in
soveryn/agents/policies.py — not scattered through app.py (spec §14).
"""
