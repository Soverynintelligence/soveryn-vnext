# CWG tax evidence

Paid receipts and insurance **bills** land here (`file_away` dest `cwg_evidence`, then `ledger_ingest` book=cwg). COI / insurance certificates are not receipts — file those on the [cwg-business](../cwg-business/README.md) shelf (`cwg_insurance`).
